# Tasks — 002-php-frontend-migration

**Status**: Draft (awaiting `/speckit-tasks`)

_To be generated by `/speckit-tasks` after the plan is approved. Run that command to produce the ordered, dependency-resolved task list._
